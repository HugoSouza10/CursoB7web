PODEMOS SEPARAR AS ROTAS:

PODEMOS CRIAR UMA PASTA CHAMADA ROUTES 
NA PASTA SRC E ASSIM COLOCAMOS UM ARQUIVO DE ROUTER

ex:


import {Router, Request, Response } from 'express';

const router = Router();

router.get('/', (req:Request, res:Response)=>{
    res.send('Olá, mundo!');
});


-PODEMOS CRIAR TAMBÉM OUTROS ARQUIVOS DE ROTAS COM /
BASTA TER ALGUM PREFIXO, EXEMPLO, /PAINEL